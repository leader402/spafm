data:
  h5ad_glob: "data/processed/visium-mouse-*/V1_Mouse_*.h5ad"
  tokenizer_config: configs/tokenizer/spafm-s-visium-mouse.yaml

embedder:
  type: hvg_mean
  top_k: 200

tasks:
  - name: cell_type
    label_key: cell_type
    cv_folds: 5
  - name: spatial_domain
    label_key: niche_label

output:
  json_path: results/benchmark-real-mouse-hvg.json

seed: 42
